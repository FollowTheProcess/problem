when:
  - event: [push]
    branch: [main]
  - event: [pull_request]
    branch: [main]

engine: nixery

dependencies:
  nixpkgs/nixpkgs-unstable:
    - go_1_27
    - golangci-lint

environment:
  CGO_ENABLED: "0"

steps:
  - name: Format
    command: golangci-lint fmt ./... --diff-colored

  - name: Lint
    command: golangci-lint run ./...
